# dsh-token-usage 功能介绍

## 是什么

一个面向 DeepSeek Harness 的本地客户端插件，用来**可视化查看 Token 用量并统计费用**。
不需要打开浏览器，不需要外部服务，直接嵌入现有客户端界面。

## 核心功能

### 1. 设置上方的轻量卡片

在侧边栏「设置」按钮上方自动显示一张小卡片，实时展示：

- 当前模型（例如 `deepseek-v4-pro`）
- 当前会话 Token 总量
- 当前会话估算费用
- 随当前会话切换自动更新

### 2. 点击卡片打开完整统计弹窗

弹窗内提供：

- **汇总卡片**
  - 总会话数
  - 总 Token
  - 输入(未缓存) Tokens
  - 缓存读取 Tokens
  - 输出 Tokens
  - 估算费用

- **按模型统计**
  - 模型名称
  - Provider
  - 会话数
  - 输入(未缓存)
  - 缓存读取
  - 输出
  - 估算费用

- **按会话统计**
  - 会话标题
  - 工作区路径
  - 使用的模型
  - Token 明细
  - 估算费用

### 3. 货币切换

- 弹窗内支持 **CNY / USD** 一键切换
- 默认使用 CNY，汇率可在代码内 `cnyPerUsd` 配置

### 4. 价格体系

- 模型价格内置自 DSH 本地 pi-ai 价格库（USD / 1M tokens）
- 离线可用，不请求外部 API
- 未定价模型会显示「未定价/免费」，不会伪造费用

### 5. 数据来源

全部通过 DSH 本地 RPC API 获取：

- `session.list`：会话列表与 Token 投影
- `session.models`：当前/各会话模型
- `session.history`：完整会话事件流，用于精确按模型聚合

## 技术特点

- 纯客户端插件，无服务端逻辑
- 通过 DSH 官方客户端模块系统加载，因此会出现在「设置 -> 插件列表」中
- 无外部依赖，不修改 DSH 核心代码
- 同时兼容一键安装脚本和手动安装
